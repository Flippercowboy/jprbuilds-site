{
  "name": "battleships-server",
  "version": "1.0.0",
  "description": "WebSocket game server for Battleships — replaces Supabase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
